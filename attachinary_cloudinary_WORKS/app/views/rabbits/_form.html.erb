<%= form_for(@rabbit) do |f| %>
  <% if @rabbit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rabbit.errors.count, "error") %> prohibited this rabbit from being saved:</h2>

      <ul>
      <% @rabbit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :building_picture %><br>
    <%= f.attachinary_file_field :building_picture %>
  </div>
  <br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
